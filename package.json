{
  "name": "taskpulse",
  "version": "1.3.3-beta",
  "private": true,
  "scripts": {
    "dev": "npm --prefix main-code run dev",
    "build": "npm --prefix main-code run build",
    "start": "npm --prefix main-code run start"
  }
}
